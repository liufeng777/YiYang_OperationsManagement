# 工作日志 2026-09-02

## 任务1：新增机构页面 + 机构详情去「同步」化改造

### 任务描述

- 用户需求 1：实现新增机构页面。机构列表点击「添加机构」跳转 create 页，保存后跳转 detail 页。表单分两块：
  - a. 基础信息：name / name_en / type / province / city / district / address / contact_phone / manager_name / manager_phone / service_radius_km
  - b. 患者端介绍：brief / description / images
- 用户需求 2：机构详情页基础信息改为可编辑；detail 页定位为「编辑 + 配置机构服务」；机构已无「同步」概念，去除所有同步相关内容。

### 解决方案

- **API 类型（institution.ts）**：删除 `SyncStatus` / `OperationStatus` / `InstitutionIntro` 及旧 `InstitutionDetail`（含 lastSyncTime / onlineRange / intro）；新 `InstitutionDetail extends InstitutionItem`，仅扩展患者端介绍字段 `introTags` / `introVisible` / `introCover?`，注释明确「无同步概念，平台直接维护」。
- **共用表单（components/InstitutionBaseFields.tsx + institution-form.less）**：抽取新增页与详情页共用的基础信息字段组（含 `institutionTypeOptions` 护理院 1 / 驿站 2、`regionOptions` 省市区 Cascader mock）；校验含 manager_phone `/^1\d{10}$/`、service_radius_km 留空不限；网格布局（grid--2 / grid--3 / --stack）+ 720px 响应式。
- **新增机构（create/index.tsx + index.less）**：PageContainer「新增机构」，Form vertical requiredMark=false；「基础信息」Card 复用 InstitutionBaseFields；「患者端介绍」Card 含 brief（必填 64 字 showCount）、description（255 字 showCount）、images（复用 `@/components/ImageSortGrid` 拖拽排序）；底部「取消 / 保存并进入配置」，validateFields 后跳转 `/institution/detail/:id`（mock，代码内 TODO 注明 payload 组装与 region 拆分方式）。
- **机构详情（detail/detail.tsx）**：mock 改为 DTO 形态（snake_case 全字段，id 1 驿站启用 / id 2 护理院停用，未知 id 回退 mock '1'）；summary 卡展示 编码 · 类型、省市区地址、contact_phone，状态 Tag 改「启用绿 / 停用红」替换原「已同步」；BaseInfoTab / PatientIntroTab 加 `key={detail.id}` 切机构重置表单。
- **基础资料 Tab（components/BaseInfoTab.tsx）**：由只读「同步资料」重写为可编辑 Form，复用 InstitutionBaseFields，initialValues 从 detail 派生（region 合并为三级数组、空值转 undefined），header 右侧「保存修改」主按钮（mock 保存）。
- **患者端介绍 Tab（components/PatientIntroTab.tsx）**：删除「协作平台同步资料」整张 Card；表单数据绑定由 `detail.intro.*` 改为 DTO 字段 `brief / description / introTags / introVisible / introCover`；预览环境照片改本地占位常量。
- **列表页（list.tsx）**：「添加机构」按钮跳转 `/institution/create`；页面描述去同步文案；修复两处筛选 Select options 类型错误（`Object.keys` 返回 string[]，改为 `([1, 2] as InstitutionType[])` / `([1, 9] as InstitutionStatus[])` 显式类型数组）。
- **路由（routes.tsx）**：institution children 的 detail 组件路径修正为 `@/pages/institution/detail/detail`（文件已迁入子目录，修复失效引用）；新增 `create` 路由（hideInMenu）。
- **ServicesTab.tsx**：删除未使用的 `SearchOutlined` 导入（其余逻辑不动）。

### 变更文件清单

| 文件路径 | 变更类型 | 说明 |
| --- | --- | --- |
| src/api/modules/institution.ts | 修改 | 删除同步相关类型；InstitutionDetail 改为 DTO 扩展 |
| src/pages/institution/components/InstitutionBaseFields.tsx | 新增 | 新增/详情共用基础信息表单字段组 |
| src/pages/institution/components/institution-form.less | 新增 | 表单网格布局与标签样式 |
| src/pages/institution/create/index.tsx | 新增 | 新增机构页（基础信息 + 患者端介绍） |
| src/pages/institution/create/index.less | 新增 | 新增机构页样式 |
| src/pages/institution/detail/detail.tsx | 修改 | DTO 形态 mock、去同步 Tag、导入路径修正 |
| src/pages/institution/components/BaseInfoTab.tsx | 重写 | 基础资料改可编辑 Form |
| src/pages/institution/components/PatientIntroTab.tsx | 修改 | 删除同步资料卡，改绑 DTO 字段 |
| src/pages/institution/components/ServicesTab.tsx | 修改 | 删除未使用的 SearchOutlined 导入 |
| src/pages/institution/list.tsx | 修改 | 添加机构跳转 create；去同步文案；Select options 类型修复 |
| src/router/routes.tsx | 修改 | detail 路径修正 + 新增 create 路由 |

### 测试验证结果

- 静态核查：全模块 grep 确认 `contactPhone / lastSyncTime / onlineRange / source / syncStatus / detail.intro.*` 旧字段引用清零；新增 less 文件所用变量（@spacing-*、@color-*、@font-size-auxiliary 等）与混入（.text-label() / .card-surface() / .text-title-card() / .text-auxiliary()）均存在于 variables.less / mixins.less，且 vite.config.ts 已全局注入。
- `tsc --noEmit`：本次执行环境 Bash 审批持续超时未能运行，需本地执行 `npm run type-check` 复核（变更均为类型内聚改造，静态核查未发现类型断点）。

### 遗留问题

- 新增 / 保存均为 mock 提交（create 跳转 id 用 `Date.now()`，detail 对未知 id 回退 mock '1'）；后端就绪后在 create/BaseInfoTab 的 TODO 处替换为 `institutionApi.createInstitution / updateInstitution`，region 拆分为 province/city/district。
- `regionOptions` 省市区为 mock 字典，待行政区划接口接入。
- 患者端介绍 Tab 的特色标签 / 展示开关 / 封面为预留 UI，待后端扩展字段。

### Git提交消息

- 提交人：Model@Trae
- 建议提交信息：`feat(institution): 新增机构页面与机构详情去同步化改造`

---

## 任务2：PageContainer 全局改为 height:100% 溢出滚动

### 任务描述

- 用户需求：修改整个项目的 PageContainer 样式，由固定视口高度改为 `height: 100%`，内容超出时高度溢出滚动。

### 解决方案

- **PageContainer（components/PageContainer/index.less）**：`height: calc(100vh - 56px)` → `height: 100%`（移除硬编码 56px），保留 flex 纵排 + `overflow: auto`。
- **高度链打通（layouts/layout.less）**：百分比高度依赖父级确定高度，故将 `.basic-layout__content` 由 `min-height` 改为 `height: calc(100vh - @header-height)`、`.page-transition` 由 `min-height: 100%` 改为 `height: 100%`；`__content` 保留 `overflow: auto` 作为未使用 PageContainer 页面的兜底滚动。
- 高度口径核对：antd 主题 `Layout.headerHeight = 56` 与 `@header-height: 56px` 一致，calc 无需调整。

### 变更文件清单

| 文件路径 | 变更类型 | 说明 |
| --- | --- | --- |
| src/components/PageContainer/index.less | 修改 | 高度改 100%，去硬编码 56px；底部 24px 留白由 `__body::after` 内容流内占位承担 |
| src/layouts/layout.less | 修改 | __content / page-transition 改确定高度，打通百分比高度链 |

### 测试验证结果

- 静态核查：DOM 链 `.basic-layout__content`（确定高度 calc(100vh - 56px)）→ `.page-transition`（height:100%）→ `.page-container`（height:100%，overflow:auto）逐级可解析；未使用 PageContainer 的页面由 `__content` 的 `overflow: auto` 兜底。
- 修复滚动到底部时 `padding: 24px` 底部不生效的问题：**根因是 `&__body` 为 flex item（`flex: 1; min-height: 0`），内容超高时 body 盒子被压缩、内容以 `overflow: visible` 溢出到盒子之外**，容器 `padding-bottom`、body 的 `padding-bottom`、以及排在 body 盒子之后的容器级 `::after` 全部被溢出内容跳过（这同样解释了改造前 `padding: 24px` 底部失效）。最终修复：容器 padding 为 `@spacing-xl @spacing-xl 0`，24px 留白由 `&__body::after`（`display: block; height: @spacing-xl`）承担——占位位于内容流内部末尾，随内容一起溢出并计入滚动区域。

### Git提交消息

- 提交人：Model@Trae
- 建议提交信息：`style(layout): PageContainer 改为 height:100% 内部溢出滚动`
