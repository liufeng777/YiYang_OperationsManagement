# 工作日志 - 2026-08-21

---

## 任务1：活动管理详情层级调整与全站面包屑重构

### 任务描述

1. 活动管理去掉「报名查询」子导航：从活动列表「查看」进入活动详情页（原报名查询页），该页不进菜单。
2. 修复面包屑：编辑活动时显示 活动管理 / 活动详情 / 编辑活动，各级可点击回跳；一级导航默认页只显示一级标题（如 活动管理）。
3. 同理校验其他页面面包屑与点击跳转。

### 交互方案

- 路由调整：/activity/detail/:id 改为活动详情页（signups），/activity/detail/:id/edit 为编辑页；编辑路径嵌套在详情之下，面包屑层级由路由结构自然推导
- findBreadcrumb 重写：将每个一级路由的所有子孙页面展开为路径模式，对当前路径逐级前缀做参数感知匹配（:id 通配），命中即补一级面包屑；默认页 '' 不再重复占位
- HeaderBar 面包屑除末级外渲染为可点击链接，navigate 直达对应路径（不经侧边栏记忆恢复，显式导航）
- findPageTitle 同步改为最深匹配，详情/编辑页 document.title 更准确

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Read/Grep | 梳理路由树、渲染入口、signups/create/list 跳转 | 确认 router 以相对路径直接渲染子 Route，多级子路径可用 |
| Edit | routes.tsx | 移除「报名查询」可见子路由；detail/:id 挂详情页，detail/:id/edit 挂编辑页 |
| Edit | list.tsx / signups.tsx / create.tsx | 查看/编辑/返回/发布后跳转全部切到新路径；signups 改 useParams 取 id |
| Edit | menu.tsx | findBreadcrumb 多级参数感知重写，返回可跳转 path；findPageTitle 同步升级 |
| Edit | HeaderBar.tsx | 面包屑非末级渲染为可点击链接 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 活动管理子路由重构：详情 detail/:id（hideInMenu）、编辑 detail/:id/edit |
| src/pages/activity/list.tsx | 代码 | 查看 -> /activity/detail/:id；编辑 -> /activity/detail/:id/edit |
| src/pages/activity/signups.tsx | 代码 | 改为 useParams 取活动 id；编辑活动按钮指向 /edit；文件头注释更新为活动详情 |
| src/pages/activity/create.tsx | 代码 | 返回按钮与发布成功后跳转区分新建/编辑（编辑回详情页） |
| src/router/menu.tsx | 代码 | findBreadcrumb 重写（多级+参数感知+可跳转 path）；findPageTitle 最深匹配 |
| src/layouts/HeaderBar.tsx | 代码 | 面包屑非末级可点击回跳 |
| WORKLOG/worklog_2026-08-21.md | 日志 | 新建本日工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 16:40:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`refactor(activity): 报名查询改为活动详情隐藏页，面包屑支持多级参数感知与点击跳转`

### 学习到的知识

- 面包屑层级交给路由路径结构推导（编辑页嵌套在详情路径下），比在面包屑函数里硬编码中间层更可维护
- 逐级前缀 + 参数段通配匹配，可同时覆盖 /order/detail/:id、/content/article/edit/:id 等各类隐藏页

### 遗留问题

- tsc/vite build 因命令审批超时未执行，已静态排查（导入、参数取值、引用完整性）；建议本地跑一次 `npm run type-check`
- 面包屑点击跳转不经导航记忆恢复（显式导航），若希望点击「活动管理」面包屑也恢复上次详情，可再讨论

### 后续待办项

- /service/detail/:id 的 meta.title 当前恒为「新建服务项目」，编辑场景可在接入接口后动态化

---

## 任务2：机构服务「编辑配置」Drawer 组件化

### 任务描述

机构详情-服务项目 Tab 中点击「编辑配置」，按设计稿用 Drawer 实现单项服务配置编辑，并提取为独立组件。

### 交互方案

- 新组件 EditServiceConfigDrawer（components/ 下，tsx + 同名 less）
- 内容：服务摘要条（集团维护只读）→ 配置方式（单项调整/继承机构默认配置）→ 线上服务范围（仅上门服务显示，按街道/电子围栏互斥禁用）→ 预约与履约规则（日容量/提前预约/接单时段/取消规则）→ 底部只读提示 Alert
- 表单用 antd Form 管理；选择「继承机构默认配置」时下方字段整体禁用
- 保存后回写服务行的配置来源（单项调整/机构默认）与线上履约范围文案

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 读取设计稿 | 明确 Drawer 结构与字段联动规则 |
| Write | EditServiceConfigDrawer.tsx / .less | Form + useWatch 联动，useEffect 打开时回填 |
| Edit | ServicesTab.tsx | 编辑配置按钮接入 Drawer；新增 handleSaveConfig；顺带删除未使用的 useNavigate（潜在 TS6133） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/pages/institution/components/EditServiceConfigDrawer.tsx | 代码（新增） | 编辑机构服务配置 Drawer（antd Form，继承时禁用、范围类型互斥） |
| src/pages/institution/components/EditServiceConfigDrawer.less | 样式（新增） | 摘要条/分区标题/双列与四列网格/底部栏样式，全部使用设计变量 |
| src/pages/institution/components/ServicesTab.tsx | 代码 | 接入编辑配置 Drawer，保存回写 configSource/range；清理未使用的 navigate |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 17:20:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(institution): 机构服务编辑配置 Drawer 组件化实现`

### 学习到的知识

- Form.useWatch 做「继承/单项」「街道/围栏」两组开关的禁用联动，比受控 state 更贴合 Form 数据流
- 编辑对象通过 open+service props 传入、useEffect 回填，组件无需感知列表数据

### 遗留问题

- tsc/vite build 因命令审批超时未执行，已静态排查；建议本地跑一次 `npm run type-check`
- 字段回填值当前为 mock 常量，接接口后按服务真实配置回填

### 后续待办项

- 服务行的上架/下架按钮目前无动作，可复用机构服务页的下架原因弹窗补齐

---

## 任务3：新建/编辑活动页对齐设计稿 + 存量类型错误修复

### 任务描述

新建/编辑活动页实现与设计稿不一致，需对齐；患者端预览弹窗同样按设计稿重构。

### 对齐项

- 活动基础信息：两列网格（活动名称/活动类型、活动摘要/活动封面）；摘要改单行输入；封面改输入框等高虚线块；卡片标题右侧补灰色辅助说明
- 报名与参与设置：三列并排（报名时间/适用人群/收费方式）；机构汇总条改双行（总数+小字说明），配置按钮改主色
- 患者端活动详情：上传条改为整行虚线横条；详情图改三列网格卡片
- 底部操作条：改为「报名须知与退款规则 + 已填写状态 + 修改（展开 TextArea）」+ 右侧三按钮的横条
- 手机预览弹窗：标题改为「患者端活动预览+副标题」；预览状态改卡片式单选（标题+描述、选中绿边）；预览范围改勾选清单；新增「仅用于展示检查」提示块；关闭预览全宽按钮入面板底部；预览状态真实联动手机画面（名额已满置灰/报名成功横幅）
- 手机预览组件：头部加返回箭头；新增信息三行（机构时间/承接人数/适人群费用）；详情图块改 DETAIL IMAGE 01/02 双色卡；费用跟随收费方式

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 对比两张设计稿 | 列出布局与组件差异 |
| Write | 重写 create.tsx / create.less | 网格布局、上传条、图片网格、操作条、预览弹窗重构 |
| Bash | tsc 全量检查 | 发现 17 个存量错误（account 遗留 setters、ServicePoolItem 缺 status、ServiceItem 缺 unit、institution/detail 未用导入） |
| Edit | 逐处修复 | account openDrawer 改 form.resetFields；mocks 补 status/unit；清理未用导入 |
| Bash | 复跑 tsc + vite build | tsc 0 错误；构建成功（1.41s） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/pages/activity/create.tsx | 代码（重写） | 对齐设计稿布局；预览状态联动；须知改为状态条+展开编辑 |
| src/pages/activity/create.less | 样式（重写） | 网格/汇总条/图片网格/操作条/预览状态卡/预览范围清单样式 |
| src/pages/system/account.tsx | 代码 | 修复 openDrawer 遗留的旧 setState 调用（改 form.resetFields） |
| src/pages/institution/components/ServicesTab.tsx | 代码 | servicePoolMocks 补 status 字段 |
| src/pages/institution/detail.tsx | 代码 | 移除未使用的 PlusOutlined 导入 |
| src/pages/service/list.tsx / detail.tsx | 代码 | mock 数据补 unit 字段 |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 18:10:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`fix(activity): 新建编辑活动页对齐设计稿并修复存量类型错误`

### 学习到的知识

- 类型接口（ServiceItem.unit、ServicePoolItem.status）被外部修改后，mock 数据会批量报错；tsc 全量检查应作为每次收尾的固定动作
- 预览类弹窗的「状态切换」用受控状态驱动同一渲染函数，比静态展示更能发现问题

### 遗留问题

- 封面/详情图上传仍为占位提示，待接入上传组件
- 详情图「拖动排序」仅为视觉按钮，未实现拖拽

### 后续待办项

- 接入上传与拖拽排序（可评估 antd Upload + dnd 方案）
- 编辑活动时各字段回填真实数据（当前仅 name/summary/notice 有 mock 回填）

---

## 任务4：详情图拖拽排序（原生 HTML5 DnD）

### 任务描述

患者端活动详情的详情图支持拖动调整顺序；不引入第三方拖拽库（遵守 AGENTS.md 依赖评估约定），用原生 HTML5 Drag & Drop 实现。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Edit | create.tsx | detailImages 转 state；dragId/dragOverId 跟踪拖拽态；drop 时 splice 重排；删除按钮可用；上传条点击追加 mock 详情图（便于演示排序） |
| Edit | create.less | .create-image 增加 grab 光标、is-dragging 半透明、is-over 品牌色描边落点提示 |
| Bash | 验证 | 审批超时未执行；已人工核对（无新增导入、状态引用完整，逻辑为纯数组重排） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/pages/activity/create.tsx | 代码 | 详情图 draggable + onDragStart/Over/Drop/End 重排；删除/上传可用化 |
| src/pages/activity/create.less | 样式 | 拖拽中半透明、拖入目标描边、grab 光标 |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 18:30:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(activity): 详情图支持原生拖拽排序，上传/删除操作可用化`

### 学习到的知识

- 简单线性列表排序用原生 DnD（dragId + dragOverId + splice）即可，无需 dnd-kit；卡片级 draggable + 落点描边的反馈足够直观

### 遗留问题

- tsc/vite build 审批超时未跑；建议本地 `npm run type-check`
- 「替换」按钮仍为占位提示；上传未接真实文件选择（antd Upload）

### 后续待办项

- 接入 antd Upload 真实上传与替换
- 触屏设备拖拽（原生 DnD 不支持移动端）如需要可再评估

---

## 任务5：图片拖拽排序组件化（ImageSortGrid）与全站统一

### 任务描述

1. 服务项目编辑页：封面与详情图片上传框同等大小（原封面宽度写死 100px）。
2. 服务详情图也支持拖动排序，交互与活动详情图一致；三个操作按钮（拖动/替换/删除）悬浮在图片底部、半透明背景；活动页按钮同步改悬浮样式。
3. 排查其他页面同类需求并保持交互一致。

### 方案

- 抽取共享组件 `src/components/ImageSortGrid`（tsx + less）：拖拽排序 + 悬浮操作条 + 可选添加块；variant=fluid（三列均分）/ fixed（132px 固定图块，与封面同尺寸）
- 排查结果：内容配置-首页轮播图（banner 行）也有排序需求，行式列表用同一原生 DnD 模式补齐拖拽（拖动把手 + 落点描边反馈一致）

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Grep | 排查图片排序场景 | 命中 activity/create、service/detail、content/home（轮播图） |
| Write | ImageSortGrid 组件 | 拖拽 + 悬浮操作条 + tone 色调变体 |
| Edit | create.tsx / create.less | 迁移到共享组件，删除本地拖拽代码与旧样式 |
| Edit | service/detail.tsx / detail.less | 封面 132×110 与详情图块同尺寸；详情图接入 ImageSortGrid(fixed) |
| Edit | content/home.tsx / home.less | 轮播图行支持拖拽排序 |
| Bash | tsc + build | 修复 SortableImage.size 可选类型冲突后全部通过 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/components/ImageSortGrid/index.tsx / index.less | 代码+样式（新增） | 共享拖拽图片网格组件 |
| src/pages/activity/create.tsx | 代码 | 图片区迁移到 ImageSortGrid；移除本地拖拽实现与 DetailImage 类型 |
| src/pages/activity/create.less | 样式 | 移除旧 create-image 样式，保留网格间距 |
| src/pages/service/detail.tsx | 代码 | 详情图状态化并接入 ImageSortGrid；封面改 button 可点击 |
| src/pages/service/detail.less | 样式 | 封面 132×110 与详情图块同尺寸；移除旧 editor-upload__images |
| src/pages/content/home.tsx / home.less | 代码+样式 | 轮播图行原生拖拽排序 |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 18:45:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(components): 抽取 ImageSortGrid 拖拽图片网格，统一活动/服务/首页轮播图排序交互`

### 学习到的知识

- 同类交互第二次出现即应组件化；悬浮操作条用 absolute + rgba(0,0,0,0.45) 统一实现
- 共享组件的 size 等字段设为可选后，注意调用方局部类型需同步放宽（TS2322）

### 遗留问题

- 上传/替换仍未接真实文件选择（antd Upload）
- 轮播图仅补齐拖拽排序，编辑仍为占位提示

### 后续待办项

- 统一接入 antd Upload（封面/详情图/轮播图）
- 快捷入口、推荐内容如设计稿确认需要排序，可复用同一 DnD 模式

---

## 任务6：图片操作按钮图标化 + 服务页展示内容布局调整

### 任务描述

1. ImageSortGrid 的拖动/替换/删除按钮只展示图标，悬浮用 Tooltip 提示文字。
2. 新建服务项目页：服务内容（TextArea）宽度固定，封面与详情图片区占满父容器剩余宽度。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Edit | ImageSortGrid/index.tsx | 操作按钮改纯图标 + antd Tooltip（含 aria-label） |
| Edit | service/detail.tsx | ImageSortGrid 去掉 fixed 变体，改默认 fluid 三列均分 |
| Edit | service/detail.less | editor-content 改为：封面 flex 1（框宽 100%）、详情图 flex 2、服务内容固定 320px；窄屏回退纵向铺满 |
| Bash | tsc + vite build | 均通过 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/components/ImageSortGrid/index.tsx | 代码 | 操作按钮图标化 + Tooltip 提示 |
| src/pages/service/detail.tsx | 代码 | 详情图网格改 fluid 填满 |
| src/pages/service/detail.less | 样式 | 封面/详情图/服务内容宽度重排（1:2:固定 320px） |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 19:00:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`style(service): 图片操作按钮图标化，展示内容区封面/详情图占满剩余宽度`

### 学习到的知识

- 图标按钮 + Tooltip 是密集操作条的通用模式，记得补 aria-label 保证可访问性

### 遗留问题

- ImageSortGrid 的 fixed 变体当前无使用方，保留备用；如下个迭代仍无场景可移除

### 后续待办项

- 统一接入 antd Upload（封面/详情图/轮播图）

---

## 任务7：服务页展示内容尺寸微调

### 任务描述

服务内容宽度改为之前的一半（320 → 160px）、高度 110px；封面与详情图片单个图块同宽。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Edit | service/detail.tsx | ImageSortGrid 改回 fixed 变体（132px 图块）；TextArea 高度 100 → 110 |
| Edit | service/detail.less | 封面框固定 132×110（与图块同尺寸）；详情图区 flex 1 自动填充；服务内容固定 160px |
| Bash | tsc + vite build | 均通过 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/pages/service/detail.tsx | 代码 | 详情图 fixed 变体；服务内容 TextArea 高 110 |
| src/pages/service/detail.less | 样式 | 封面 132×110 与详情图块同宽；服务内容 160px 固定宽 |
| WORKLOG/worklog_2026-08-21.md | 日志 | 追加本条目 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-21 19:15:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`style(service): 展示内容区封面与详情图同宽，服务内容调整为 160×110`

### 学习到的知识

- 同一区域尺寸多次调整说明视觉仍在打磨；图块尺寸收敛到 ImageSortGrid 的 fixed 变体一处，封面只需跟随即可

### 遗留问题

- 无新增

### 后续待办项

- 统一接入 antd Upload（封面/详情图/轮播图）

---
