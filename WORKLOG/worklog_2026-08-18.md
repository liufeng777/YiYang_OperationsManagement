# 工作日志 - 2026-08-18

---

## 任务6：订单中心（订单列表 + 订单详情）

### 任务描述

按 2 张订单中心设计稿（订单列表、订单详情）实现订单中心模块，含状态统计、筛选、状态 Tabs、催办/审核操作与订单详情页（履约进度、当前操作、接单时效、操作记录）。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 读取订单列表、订单详情 2 张设计稿 |
| Read/Edit/Write | 修改代码 | 路由标题、API 类型、订单列表页、订单详情页 |
| Bash | 验证构建 | `tsc --noEmit`、`vite build`（node 直接调用，均通过） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 一级导航「退款详情」更名「退款管理」，对齐设计稿侧边栏 |
| src/api/modules/order.ts | 代码 | OrderStatus / WorkOrderStatus / OrderItem / OrderLog / OrderDetail 类型与 urgeOrder、cancelOrder 等签名 |
| src/pages/order/list.tsx / list.less | 代码/样式 | 订单列表整页：4 指标卡（图标 + 彩色备注 pill）+ 筛选 + 工单状态 Tabs + 表格，操作列按状态区分（详情/催办/审核） |
| src/pages/order/detail.tsx / detail.less | 代码/样式 | 订单详情整页：状态摘要条 + 交易订单信息 + 工单履约 5 步进度 + 当前操作（催办/更换机构/取消订单）+ 接单时效倒计时 + 服务备注 + 操作记录表 |
| WORKLOG/worklog_2026-08-17.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 09:40:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(order): 按设计稿实现订单中心列表与订单详情`

### 学习到的知识

- 订单列表操作列由「订单状态 + 工单状态」双维度决定：待机构接单显示催办、退款审核显示审核（跳转退款管理）
- 履约进度步骤条用网格卡片 + is-done/is-current 修饰类实现，无需引入 antd Steps 即可对齐设计稿视觉

### 遗留问题

- 订单列表与详情均为 mock 数据；催办、取消订单、更换机构为 mock 交互
- 接单时效倒计时为静态展示（00:18:24），未做真实秒级倒计时
- 订单详情 mock 按 id 映射基础信息，交易/工单明细数据暂共用同一套（接入后端后按订单返回）

### 后续待办项

- 接入订单中心真实接口（列表、详情、催办、取消、导出）
- 继续按设计稿实现退款管理等剩余模块

---

## 任务7：退款管理（退款列表 + 退款审核详情）

### 任务描述

按 2 张退款管理设计稿（退款列表、退款审核详情）实现退款管理模块，含状态统计、筛选、状态 Tabs、审核/驳回/处理异常操作与审核详情页（协作平台联动、审核时效、联动规则、审核记录）。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 读取退款列表、退款审核详情 2 张设计稿 |
| Read/Edit/Write | 修改代码 | 路由标题、API 类型、退款列表页、退款审核详情页 |
| Bash | 验证构建 | `tsc --noEmit`（修复 1 处未使用变量后通过）、`vite build` 通过 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 退款管理二级项更名：退款列表 / 退款审核详情（隐藏路由） |
| src/api/modules/refund.ts | 代码 | RefundStatus / CancelStatus / RefundItem / RefundLog / RefundDetail 类型与 processRefund、resolveRefundAbnormal 等签名 |
| src/pages/refund/list.tsx / list.less | 代码/样式 | 退款列表整页：4 指标卡 + 筛选（退款单号/订单号/用户、机构、退款状态、工单取消状态、申请时间）+ 状态 Tabs + 表格，操作列按状态区分（审核/查看/处理异常） |
| src/pages/refund/detail.tsx / detail.less | 代码/样式 | 退款审核详情整页：状态摘要条 + 整单退款申请 + 协作平台联动 5 步进度 + 审核操作（同意整单退款/驳回申请/查看原订单，异常态为重试取消/转人工）+ 审核时效 + 联动规则 + 审核记录表 + 驳回原因 Modal |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 09:52:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(refund): 按设计稿实现退款管理列表与审核详情`

### 学习到的知识

- 退款审核与订单履约强联动：审核通过 → 取消全部关联工单 → 全部成功后原路退款；任一工单不可取消进入异常处理，详情页操作区需按状态切换（审核/异常/只读）
- 驳回申请需强制填写原因（≥5 字）并同步用户，与服务停用的原因表单约束一致

### 遗留问题

- 退款列表与审核详情均为 mock 数据；同意/驳回/重试取消/转人工为 mock 交互
- 审核时效倒计时为静态展示（01:42:36），未做真实秒级倒计时
- 详情页 mock 按 id 映射基础信息，申请明细与记录暂共用同一套（接入后端后按退款单返回）

### 后续待办项

- 接入退款管理真实接口（列表、详情、审核、驳回、异常处理）
- 内容配置 / 系统设置模块如有设计稿可继续按稿实现

---

## 任务8：财务对账（侧边栏新增 + 对账汇总列表）

### 任务描述

侧边栏新增「财务对账」一级导航（位于退款管理与内容配置之间），并按设计稿实现对账汇总页面：金额统计、筛选、状态 Tabs、核对 / 处理差异 / 导出对账单操作。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 读取财务对账列表设计稿 |
| Read/Edit/Write | 修改代码 | 路由、API 出口与类型、对账汇总列表页 |
| Bash | 验证构建 | `tsc --noEmit`、`vite build`（node 直接调用，均通过） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 新增一级导航 /finance「财务对账」（AccountBookOutlined），单一二级页 |
| src/api/index.ts | 配置 | 新增 financeApi 出口 |
| src/api/modules/finance.ts | 代码 | ReconcileStatus / ReconcileItem / ReconcileSummary 类型与 getReconcileList、confirmReconcile、resolveReconcileDiff、exportReconcile 等签名 |
| src/pages/finance/list.tsx / list.less | 代码/样式 | 对账汇总整页：4 金额指标卡 + 筛选（日期/机构/批次、机构、对账状态、支付渠道、对账日期）+ 状态 Tabs（全部/待核对/已核对/差异/今日/本月）+ 表格，操作列按状态区分（核对/查看/处理差异），核对与差异处理均带确认弹窗并 mock 更新状态 |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 10:15:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(finance): 新增财务对账导航与对账汇总列表`

### 学习到的知识

- 对账粒度为「日期 × 机构 × 支付渠道」，列表同时展示支付笔数/退款笔数与支付/净收金额，金额统一用千分位 + 两位小数格式化
- 差异处理与核对共用「确认后状态流转」模式，差异单需人工确认后方可标记已核对

### 遗留问题

- 对账汇总数据为 mock；导出对账单、核对、处理差异为 mock 交互
- 设计稿无对账明细页，「查看」操作暂以消息提示展示汇总信息，待补充明细设计

### 后续待办项

- 接入财务对账真实接口（汇总、明细、确认核对、差异处理、导出）
- 如对账差异需明细对照页，等产品补充设计稿后实现

---

## 任务9：内容配置（首页配置 + 科普内容 + 专业人员）

### 任务描述

按 5 张内容配置设计稿（患者端首页配置、科普内容列表、新建编辑科普内容、专业人员展示、专业人员展示设置）实现内容配置模块，并调整侧边栏二级菜单与路由。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 读取 5 张内容配置设计稿 |
| Read/Edit/Write | 修改代码 | 路由、API 类型、首页配置页、科普列表/编辑页、专业人员页 |
| Bash | 验证构建 | `tsc --noEmit`、`vite build`（node 直接调用，均通过） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 内容配置二级菜单重做：首页配置 / 科普内容 / 专业人员；科普编辑（article/edit/:id）为隐藏路由 |
| src/api/modules/content.ts | 代码 | BannerItem / HomeEntryItem / HomeRecommendItem / ArticleItem / StaffItem 类型与首页配置、科普、人员展示相关接口签名 |
| src/pages/content/home.tsx / home.less | 代码/样式 | 患者端首页配置：轮播图列表（删除 mock 生效）+ 固定快捷入口 + 推荐内容（Tabs + 移除自动保存）+ 右侧患者端首页实时预览手机 |
| src/pages/content/article.tsx / article.less | 代码/样式 | 科普内容管理：筛选 + 状态文字 Tabs + 文章表格（行选择、批量发布/下架、编辑/预览/下架/发布/重新发布按状态切换）+ 底部同步提示条 |
| src/pages/content/article-edit.tsx / article-edit.less | 代码/样式 | 新建/编辑科普内容：基础信息表单 + 图文正文编辑器（工具栏占位）+ 发布设置（推荐开关/发布时间/保存草稿）+ 患者端文章实时预览手机 |
| src/pages/content/staff.tsx / staff.less | 代码/样式 | 专业人员展示：资料来源提示条 + 筛选 + 状态统计 + 人员表格（批量展示/隐藏/设置首页推荐）+ 人员展示设置 Drawer（只读工作台资料 + 展示开关/推荐/顺序/简介/卡片预览） |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 10:35:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(content): 按设计稿实现首页配置、科普内容与专业人员展示`

### 学习到的知识

- 设计稿中内容配置侧边栏为「内容管理（科普内容/专业人员）+ 内容配置（首页配置）」分组展示，现有两级菜单系统暂以平铺三项呈现
- 专业人员资料为工作台只读同步，运营侧只能调展示层（开关/推荐/顺序/简介），Drawer 中用「只读」标签明确数据边界

### 遗留问题

- 全部页面为 mock 数据；轮播图/封面上传、拖动排序、富文本编辑为静态占位
- 旧 `src/pages/content/banner.tsx`、`copy.tsx` 已不再被路由引用，因本地删除命令未获批准暂保留（死代码，无打包影响）
- 科普「预览」操作暂为提示，待补充预览弹窗设计

### 后续待办项

- 接入内容配置真实接口（首页配置读取/发布、科普 CRUD 与上下架、人员同步与展示设置）
- 系统设置模块如有设计稿可继续按稿实现

---

## 任务10：系统设置（用户/角色/消息/公告/帮助/协议）

### 任务描述

按 8 张系统设置设计稿（用户管理、新增用户 Drawer、角色管理、消息模板与系统公告、帮助与电话配置、协议与授权内容、新建协议版本等）重做系统设置模块，调整侧边栏二级菜单与路由，并补充页内 Tab 共享组件。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 读取 8 张系统设置设计稿（已归档 design-ref/system/） |
| Read/Edit/Write | 修改代码 | 路由、API 类型与出口、共享 Tab 组件、6 个二级页面 + 协议编辑隐藏页 |
| Bash | 验证构建 | `tsc --noEmit`、`vite build`（node 直接调用，均通过） |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/router/routes.tsx | 配置 | 系统设置二级菜单重做：用户管理 / 角色管理 / 协议与授权 / 帮助与电话 / 消息模板 / 系统公告 / 操作日志；协议编辑（agreement/edit/:id）为隐藏路由 |
| src/api/modules/system.ts | 代码 | AccountItem / RoleItem / MessageTemplateItem / AnnouncementItem / FaqItem / AgreementItem / LogItem 类型与账号、角色、模板、公告、帮助、协议、日志接口签名 |
| src/pages/system/components/SystemTabs.tsx / system-tabs.less | 代码/样式 | 系统设置页内共享 Tab（平台账号/角色权限/协议与授权/消息模板/帮助与电话/操作日志），按路由前缀高亮跳转 |
| src/pages/system/account.tsx / account.less | 代码/样式 | 用户管理：4 指标卡 + 筛选 + 角色 Tabs + 用户表格 + 新增用户 Drawer（权限摘要随角色联动），创建/停用 mock 生效 |
| src/pages/system/role.tsx / role.less | 代码/样式 | 角色管理：左侧角色列表 + 右侧 6 组权限 pill 勾选，平台管理员内置只读，支持恢复默认/保存权限 |
| src/pages/system/message.tsx / message.less | 代码/样式 | 业务消息模板：5 条模板 + 渠道 pill（站内/微信/短信）+ 编辑 Drawer 占位 |
| src/pages/system/announcement.tsx / announcement.less | 代码/样式 | 系统公告：使用边界提示 + 公告表格，操作按状态切换（预览/结束、编辑/预览、编辑/发布、预览/复制），结束与发布 mock 生效；与消息模板页互跳子 Tab |
| src/pages/system/help.tsx / help.less | 代码/样式 | 帮助与电话：客服电话与服务时间配置 + 分流规则 + 患者端帮助中心预览手机 + 常见问题表格（隐藏 mock 生效） |
| src/pages/system/agreement.tsx / agreement.less | 代码/样式 | 协议与授权：版本规则提示 + 筛选 + 协议版本表格（发布 mock 生效） |
| src/pages/system/agreement-edit.tsx / agreement-edit.less | 代码/样式 | 新建协议版本：基本信息 + 正文编辑器占位 + 发布设置 + 患者端协议预览手机（含「我已阅读并同意」按钮） |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 11:25:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`feat(system): 按设计稿实现用户/角色/消息/公告/帮助/协议模块`

### 学习到的知识

- 系统设置各页共用同一套页内 Tab，抽为 SystemTabs 共享组件，按路由前缀高亮，避免每页重复维护
- 角色权限用「权限组 + pill 多选」呈现比树形控件更贴近设计稿；内置角色（平台管理员）需整体只读
- 公告与消息模板在一期合并为「消息与公告」入口的上下级 Tab，操作按钮文案随状态机切换（草稿→待生效→展示中→已结束）

### 遗留问题

- 全部页面为 mock 数据；新增用户、保存权限、发布公告等为 mock 交互
- 富文本编辑器、文件上传为静态占位，待选型接入
- 旧操作日志页 `log.tsx` 因无新设计稿仅保留原实现，仍挂在新菜单下

### 后续待办项

- 接入系统设置真实接口（账号/角色/权限、模板、公告、帮助配置、协议版本）
- 操作日志页待产品补充设计稿后按稿重做

---

## 任务11：指标卡片样式提取为公共样式

### 任务描述

运营首页、机构管理、报名查询、订单中心、退款管理、财务对账、用户管理七个页面的指标卡片样式重复定义，提取为公共 less 样式，各页面复用统一类名。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Read/Grep | 梳理差异 | 确认四种形态：带图标+标记（订单/退款/财务/用户）、圆形图标（机构）、数值同行（报名查询）、Card 布局（运营首页） |
| Write/Edit | 提取与迁移 | 新建公共样式并全局引入；迁移 7 个页面的 tsx 类名与 less |
| Bash | 验证构建 | `tsc --noEmit`、`vite build` 均通过；构建产物 CSS 中确认公共类生效 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/styles/metric-card.less | 样式（新增） | 公共指标卡：`.metric-cards` 网格容器（含 1200px/720px 响应式）+ `.metric-card`（__head/__label/__icon[--round]/__value/__row/__extra/__note/__badge，primary/info/warning/danger 四色调统一） |
| src/styles/global.less | 样式 | 引入 metric-card.less |
| src/pages/dashboard/index.tsx / index.less | 代码/样式 | 指标卡迁移公共类，页面级差异收敛为 `.metric-card--dashboard` 修饰符（Card 内边距、40px 行高、text-label 标签） |
| src/pages/institution/list.tsx / list.less | 代码/样式 | 迁移公共类（`__icon--round` 圆形图标），页面仅保留 padding 与标记间距两条覆盖 |
| src/pages/activity/signups.tsx / signups.less | 代码/样式 | 迁移公共类，使用 `__row` + `__extra` 数值同行结构 |
| src/pages/order/list.tsx / list.less、refund/list.tsx / list.less、finance/list.tsx / list.less、system/account.tsx / account.less | 代码/样式 | 四个页面相同的 ~90 行指标卡 less 块整体删除，迁移公共类；媒体查询仅保留筛选区规则 |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 14:30:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`refactor(styles): 提取指标卡片公共样式并在七个页面复用`

### 学习到的知识

- 七个页面的指标卡实为四种结构变体，统一为 `.metric-card` 解剖结构（head/label/icon/value/row/extra/note/badge）+ 修饰符（--round、--dashboard）后，单页只需保留少量覆盖
- vite additionalData 只注入编译入口文件；被 global.less `@import` 的公共样式可直接使用 variables/mixins（global.less 顶部已显式引入）
- 机构管理 warning 图标色调（fade 16%/原色）与运营首页（fade 22%/darken 20%）存在细微差异，本次统一为主流规范 fade 18%/darken 18%

### 遗留问题

- 集团服务池（service/list）与活动列表（activity/list）也有同构指标卡，不在本次范围内，未迁移
- 机构管理 warning 图标色调统一后与原件有轻微色差，如设计稿要求保留可在页面 less 覆盖

### 后续待办项

- 如需要，将服务池 / 活动列表指标卡一并迁移到公共类
- 后续新增带指标卡的页面直接使用 `.metric-cards` + `.metric-card` 公共类

---

## 任务12：筛选区与列表卡片样式统一提取

### 任务描述

各列表页过滤条件样式统一为横向排布、gap 12px；列表卡片统一 ant-card-body padding 20px 16px、列表标题 16px / 字重 700，提取为公共样式各页面复用。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| Read/Grep | 梳理现状 | 发现各页筛选区 grid 模板写在 Card 根类上（未作用于 ant-card-body，横向布局实际失效），列表卡 padding 同样叠加在根节点 |
| Write/Edit | 提取与迁移 | 新建 list-page.less 并全局引入；迁移 13 个列表页 tsx 类名与 less |
| Bash | 验证构建 | `tsc --noEmit`、`vite build` 均通过；构建产物 CSS 确认公共类生效 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/styles/list-page.less | 样式（新增） | `.filter-bar`（筛选卡：body 横向 flex + wrap + gap 12px，搜索框 240px 基准更宽、下拉/日期 140px 基准、按钮自适应）+ `.filter-row`（复合筛选卡内筛选行，科普/专业人员复用）+ `.list-card`（卡片表面 + body padding 20px 16px + 表头 th 样式）+ `.list-card__header`（标题 16px/700 + 辅助说明 + Tabs 防压缩） |
| src/styles/global.less | 样式 | 引入 list-page.less |
| order / refund / finance / activity(list+signups) / system(account) 的 list.tsx + list.less | 代码/样式 | 筛选卡 `filter-bar + 页面类（仅留 margin-top）`；表格卡与头部迁移 `list-card / list-card__header`；删除本地 grid 模板与失效媒体查询 |
| system/agreement / announcement / message（tsx+less） | 代码/样式 | 同上（筛选区上方有 banner，无需 margin-top，直接用 filter-bar） |
| institution/list.tsx + list.less | 代码/样式 | 筛选卡与表格卡迁移公共类；保留 tbody td vertical-align:top 覆盖；页内 flex gap 已提供间距故 list-card margin-top 归零 |
| service/list.tsx + list.less | 代码/样式 | 服务池筛选/表格迁移公共类；媒体查询仅保留 metrics 与 main 双列布局 |
| content/article / staff（tsx+less） | 代码/样式 | 复合筛选卡保留状态行（filter-tabs/filter-stats），筛选行改由全局 .filter-row 接管，body padding 移入 ant-card-body；表格迁移公共类 |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 15:35:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`refactor(styles): 统一筛选区横向布局与列表卡片样式并提取公共类`

### 学习到的知识

- antd Card 的 className 挂在根节点，布局/内边距需作用于 `.ant-card-body`；此前多页把 grid 模板写在根类上，筛选条件实际并未横向排布，本次随统一一并修复
- 列表页标题原混用 `.text-title-card()`（16/Medium）与 `.text-title-module()`（20/Medium），按需求统一为 16px / 700
- 筛选项尺寸规范：搜索框 flex 1 1 240px，下拉/日期 flex 1 1 140px，按钮不伸缩，flex-wrap 天然替代原 1200px/720px 媒体查询

### 遗留问题

- 详情/编辑类页面（订单详情、退款详情、活动新建等）为双栏布局，不属于列表页模式，未迁移
- 帮助与电话（FAQ 表）、操作日志等配置型页面未按列表卡迁移，如需统一可后续处理

### 后续待办项

- 新增列表页直接使用 `.filter-bar` + `.list-card` + `.list-card__header`
- 如需详情页卡片 body padding 也统一为 20px 16px，可再提取 `.detail-card` 公共类

---

## 任务13：列表头部状态筛选 Tabs → Radio.Group 胶囊样式

### 任务描述

将活动列表、报名查询、财务对账、订单中心、退款管理、用户管理六个页面列表卡片头部的 `<Tabs>` 替换为 `<Radio.Group>`（optionType="button"），样式对齐设计稿的胶囊筛选（全部 / 草稿 3 / 待发布 3 …），统一样式沉淀到 list-card.less。

### 执行步骤

| 工具 | 用途 | 结果 |
| ---- | ---- | ---- |
| ReadMediaFile | 查看设计稿 | 活动列表设计稿：头部右侧为独立胶囊单选组，选中胶囊品牌浅底 + 品牌色描边 |
| Edit | 替换组件 | 6 个页面 antd 导入 Tabs→Radio，头部 JSX 替换为 Radio.Group（options 由 tabItems 映射 value/label） |
| Edit | 公共样式 | list-card.less 新增 `.list-card__status-filter` 胶囊样式，头部 `> .ant-tabs` 规则同步替换 |
| Bash | 验证构建 | `tsc --noEmit`、`vite build` 均通过 |

### 文件更新

| 文件 | 类型 | 更新内容 |
| ---- | ---- | -------- |
| src/styles/list-card.less | 样式 | 新增 `.list-card__status-filter`：28px 高全圆角胶囊、1px 边框、gap 8px、去分段竖线、选中态品牌浅底 + 品牌色文字描边；顺带修正 `.list-card` body padding 变量顺序为 `@spacing-lg @spacing-md`（20px 16px，此前文件内注释与值不一致） |
| activity/list.tsx、activity/signups.tsx、finance/list.tsx、order/list.tsx、refund/list.tsx、system/account.tsx | 代码 | `<Tabs activeKey items onChange>` → `<Radio.Group className="list-card__status-filter" optionType="button" value onChange options>` |
| WORKLOG/worklog_2026-08-18.md | 日志 | 追加本任务工作日志 |

### Git提交消息

- 提交人：Kimi@Trae
- 时间：2026-08-18 16:40:00
- 未提交（等待用户确认后提交）
- 建议提交信息：`refactor(list): 列表状态筛选 Tabs 改为 Radio.Group 胶囊样式并沉淀公共样式`

### 学习到的知识

- Radio.Group optionType="button" 默认是分段按钮组（相邻合并边框、首尾才圆角），改胶囊需 `::before{display:none}` + `margin-left:0` + 全部 border-radius-full + gap 分隔
- tabItems 仍保留 `{key,label}` 结构，通过 options 映射 `{value,label}`，筛选逻辑零改动
- list-card.less 已被重构为 `&__header` 嵌套 + `__title/__tips/__link` 显式类名，本次在其结构上追加

### 遗留问题

- 内容配置首页（content/home）推荐内容区仍用 Tabs，属配置面板而非列表筛选，未改动
- 胶囊选中态按品牌色规范实现（浅绿底 + 品牌色描边），与设计稿深色描边略有差异，如需完全一致可调整

### 后续待办项

- 其他列表页若加状态筛选，直接复用 `.list-card__status-filter`

---
